<div class="row">
  <div class="span9">
    <%= image_tag(@envelop.picture_url, :width => "100%") if @envelop.picture.present? %>
  </div>
  
  
    <div class="span3">
    <p><b>Name: </b><%= @envelop.name %></p>
    <p><b>Description: </b><%= @envelop.description %></p>
    <p>
      <%= link_to 'Edit', edit_envelop_path(@envelop) %> |
      <%= link_to 'Destroy', @envelop, confirm: 'Are you sure?', method: :delete %> |
      <%= link_to 'Back', envelops_path %>
    </p>
       <h3>Comments</h3>
<% @comments.each do |comment| %>
  <div>
    <strong><%= comment.user_name %></strong>
    <br />
    <p><%= comment.body %></p>
    <p><%= link_to 'Delete', comment_path(comment), method: :delete, data: { confirm: 'Are you sure?' } %></p>
  </div>
<% end %>

<%= render 'comments/form' %>

<%= link_to 'Edit', edit_envelop_path(@envelop) %> |
<%= link_to 'Back', envelops_path %>

  </div>
  



</p>


</div>